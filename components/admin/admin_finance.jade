template(name='te_finance_nav')
    .ui.attached.borderless.inverted.tiny.menu.topnav(style="background:#{current_tribe.nav_color}80")
        a.item(href="/t/#{current_tribe.slug}/admin/finance/summary") 
            +i name='dashboard' classes='ui avatar image'
            |summary
        a.item(href="/t/#{current_tribe.slug}/admin/finance/invoices") 
            +i name='list' classes='ui avatar image'
            |invoices
        a.item(href="/t/#{current_tribe.slug}/admin/finance/payments_refunds") 
            +i name='search' classes='ui avatar image'
            |payments & refunds
        a.item(href="/t/#{current_tribe.slug}/admin/finance/reports") 
            +i name='signal' classes='ui avatar image'
            |reports
        a.item(href="/t/#{current_tribe.slug}/admin/finance/audit_log") 
            +i name='groups' classes='ui avatar image'
            |audit log
        a.item(href="/t/#{current_tribe.slug}/admin/finance/taxes") 
            +i name='user-female' classes='ui avatar image'
            |taxes
        a.item(href="/t/#{current_tribe.slug}/admin/finance/tenders") 
            +i name='envelope' classes='ui avatar image'
            |tenders
        a.item(href="/t/#{current_tribe.slug}/admin/finance/quickbooks") 
            +i name='id-card' classes='ui avatar image'
            |quickbooks
        a.item(href="/t/#{current_tribe.slug}/admin/finance/finance_settings") 
            +i name='settings' classes='ui avatar image'
            |invoice and receipt settings
     

template(name='admin_finance_summary')
    .ui.padded.stackable.grid
        .sixteen.wide.column
            .ui.header 
                +i name='dashboard'
                |summary

template(name='admin_finance')
    a#add_user.ui.basic.button(href='/user/add')
        i.plus.icon
        |add member
    table.ui.sortable.striped.table
        thead
            tr
                th image
                th profile
                th roles
                th actions
        tbody
            each users
                tr
                    td 
                        img.ui.small.image(src="{{c.url image_id width=1000 height=1000 gravity='face' crop='crop'}}")
                    td 
                        a.ui.header(href="/user/#{username}")
                            |#{first_name} #{last_name}
                        .ui.small.header @#{username}
                        each emails
                            |#{address}
                    td 
                        +multi_doc_edit key='roles' ref_model='role' direct=true
                        //- +user_role_toggle role='admin'
                        //- +user_role_toggle role='staff'
                        //- +user_role_toggle role='resident'
                        //- +user_role_toggle role='owner'
                        //- +user_role_toggle role='guest'
                    td
                        a.ui.icon.large.basic.button(href="/user/#{username}/edit")
                            i.pencil.icon
                        a.ui.icon.large.basic.button(href="/user/#{username}/")
                            i.chevron.right.icon
                        

template(name='admin_finance_invoices')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
        .ui.header admin_finance_invoices
template(name='admin_finance_taxes')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
        .ui.header admin_finance_taxes
template(name='admin_finance_payments_refunds')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
        .ui.header admin_finance_payments_refunds
        
        
template(name='admin_finance_reports')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
        .ui.header admin_finance_reports
        
        
        
template(name='admin_finance_audit_log')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
                .ui.inline.header
                    +i name='groups'
                    |groups
                .ui.icon.button.add_group
                    i.plus.icon
        .row
            .eight.wide.column
                .ui.header groups
                each groups
                    .ui.pointer.segment.select_group
                        .ui.header #{title}
                        |#{description}   
                        .ui.small.header #{member_usernames.length} finance 
                        each member_usernames
                            .ui.basic.label #{this}    
            .eight.wide.column
                if editing_group
                    with editing_group_doc
                        .ui.segment
                            +text_edit key='title' label='title' direct=true
                            +textarea_edit key='description' label='description' direct=true
                            +multi_user_edit key='member_usernames' direct=true



template(name='admin_finance_tenders')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
        .ui.header admin_finance_tenders
        
        
        
template(name='admin_finance_quickbooks')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
        .ui.header admin_finance_quickbooks
        
        
        
template(name='admin_finance_finance_settings')
    .ui.stackable.padded.grid
        .row
            .sixteen.wide.column
        .ui.header admin_finance_finance_settings
