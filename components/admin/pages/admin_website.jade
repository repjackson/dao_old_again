template(name='te_website_nav')
    .ui.attached.borderless.inverted.grey.small.menu.topnav
        a.item(href="/t/#{current_tribe.slug}/admin/website/pages")
            +i name='template' classes='ui avatar image'
            |site pages
        a.item(href="/t/#{current_tribe.slug}/admin/website/templates")
            +i name='template' classes='ui avatar image'
            |page templates
        a.item(href="/t/#{current_tribe.slug}/admin/website/system_pages")
            +i name='system-information' classes='ui avatar image'
            |system pages
        a.item(href="/t/#{current_tribe.slug}/admin/website/files")
            +i name='file' classes='ui avatar image'
            |files
        a.item(href="/t/#{current_tribe.slug}/admin/website/styles")
            +i name='change-theme' classes='ui avatar image'
            |colors and styles
        a.item(href="/t/#{current_tribe.slug}/admin/website/themes")
            +i name='paint' classes='ui avatar image'
            |theme
        a.item(href="/t/#{current_tribe.slug}/admin/website/css")
            +i name='css' classes='ui avatar image'
            |css
        a.item(href="/t/#{current_tribe.slug}/admin/website/overrides")
            +i name='template' classes='ui avatar image'
            |theme overrides
        a.item(href="/t/#{current_tribe.slug}/admin/website/trash")
            +i name='trash' classes='ui avatar image'
            |trash
                   



template(name='admin_website')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='template'
                    |tribe website
                .ui.button.add_page(title='add page')
                    i.plus.icon
                    |add page
                .ui.vertical.fluid.large.menu
                    each pages
                        .item.pointer.set_page #{title}
                            if home
                                i.home.icon
            .twelve.wide.column
                +tribe_page_view slug=slug





template(name='tribe_page_view')
    with current_page
        .ui.three.compact.buttons
            .ui.button.set_view_mode(class=view_mode_class)
                +i name='template' classes='ui inline mini image'
            .ui.button.set_edit_page(class=edit_mode_class)
                +i name='pencil' classes='ui inline mini image'
            .ui.button.set_configure_page(class=configure_mode_class)
                +i name='settings' classes='ui inline mini image'
        .ui.header #{title}
        if viewing
            +text_view key='title' label='title' direct=true
            +html_view key='content' label='content' direct=true
            +boolean_view key='home' label='home' direct=true
        else if editing
            +text_edit key='title' label='title' direct=true
            +html_edit key='content' label='content' direct=true
            +boolean_edit key='home' label='home' direct=true
        else if configuring
            .ui.header 
                +i name='settings' 
                |configure
            +text_view key='title' label='title' direct=true
            +text_view key='url' label='url' direct=true
            +boolean_view key='home' label='home' direct=true




template(name='admin_website_pages')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='template'
                    | website pages
template(name='admin_website_css')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='css'
                    | website css
template(name='admin_website_templates')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='template'
                    | website templates
template(name='admin_website_files')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='file'
                    | website files
template(name='admin_website_styles')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='styles'
                    | website styles
template(name='admin_website_themes')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='themes'
                    | website themes
template(name='admin_website_overrides')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='overrides'
                    | website overrides
template(name='admin_website_trash')
    .ui.stackable.padded.grid
        .row
            .four.wide.column
                .ui.inline.header 
                    +i name='trash'
                    | website trash
