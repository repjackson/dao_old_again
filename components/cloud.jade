template(name='cloud')
    //- #cloud
        //- .ui.left.icon.input
        //-     i.search.icon
        //-     +inputAutocomplete settings=settings id="search" class="" autocomplete="off" 
        // .ui.hidden.fitted.divider
    if is_eric
        .ui.icon.button.add_doc
            i.plus.icon
    if selected_tags
        #clear_tags.ui.active.icon.big.button
            i.remove.icon
        each selected_tags
            .unselect_tag.ui.active.big.button
                strong #{this}
    each all_tags
        .ui.big.compact.button.select_tag(class=cloud_tag_class) 
            strong #{this.name}
